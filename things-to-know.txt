About selenium:
1. While catching items using the By class, use . instead of space if the locator of the element has spaces.
For example, 'btn.btn-warning.webpage-extend' instead of 'btn btn-warning webpage_extend'.